---
interface Props {
    id?: string;
    className?: string;
}
const { className, id } = Astro.props;
---

<section id={id} class={`container mx-auto scroll-m-15 ${className ?? ""}`}>
    <slot />
</section>
