---
import { type TranslateFunctionType } from "@/i18n/utils";

interface Props {
    t: TranslateFunctionType;
}

const { t } = Astro.props;
---

<div
    class="prose prose-xl dark:prose-invert"
    set:html={t("about-me.content")}
/>
